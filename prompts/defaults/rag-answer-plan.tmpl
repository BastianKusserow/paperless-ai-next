You are planning how to answer a document question.

Today is {{ context.currentDate }}.
Language hint: {{ context.languageHint }}.

Question:
{{ context.question }}

Available sources:
{% for src in context.sources %}
[{{ src.index }}] {{ src.title }}{% if src.correspondent %} - {{ src.correspondent }}{% endif %}{% if src.date %} - {{ src.date }}{% endif %}{% if src.tags %} - Tags: {{ src.tags }}{% endif %}
Snippet: {{ src.snippet }}

{% endfor %}

Return JSON only with this exact schema:
{
  "answer": "short answer based only on the snippets and metadata",
  "citations": [1],
  "needs_deeper_evidence": false,
  "required_sources": [],
  "reason": "why deeper evidence is or is not needed",
  "confidence": "high|medium|low"
}

Rules:
- Answer from snippets and metadata only.
- If exact wording, exact totals, exact dates, payment determination, or legal wording requires more context, set needs_deeper_evidence to true.
- required_sources must contain only the source numbers that need full document text.
- Do not request every source unless it is truly necessary.
- Keep the answer concise and factual.
